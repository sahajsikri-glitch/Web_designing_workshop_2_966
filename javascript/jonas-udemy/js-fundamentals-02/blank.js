//function :
//3 step process :

//01.Home-Work from gpt :
"use strict";
function sum(a, b) {
  const add = a + b;
  return add;
}
console.log(sum(3, 7));

const displayName = (naam) => console.log(naam);
displayName("Sahaj");

const areaOfRectangle = function area(length, breadth) {
  return length * breadth;
};
const displayArea = (a, b) => areaOfRectangle(a, b);

console.log(displayArea(1, 2));

//02.Coding-Challenge by jonas sir
// DONE WITH CODING CHALLENGE #!

//03.2-3 tiny projects (real-world oriented)
